# Pipecat voice pipeline — Hebrew real-time STT → LLM → TTS with barge-in.
# Pin minor versions on build-day after testing Hebrew turn-taking.
pipecat-ai[deepgram,cartesia,google,anthropic,silero]>=0.0.50
python-dotenv>=1.0
httpx>=0.27
# transport (choose on build-day):
#   - websocket bridge from Asterisk (audiosocket / external media), or
#   - livekit / daily transport. Add the matching extra then.
